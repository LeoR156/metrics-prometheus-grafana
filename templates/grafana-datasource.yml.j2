apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    basicAuth: true
    basicAuthUser: {{ prom_admin_user }}
    secureJsonData:
      basicAuthPassword: {{ prom_admin_password }}
    isDefault: true